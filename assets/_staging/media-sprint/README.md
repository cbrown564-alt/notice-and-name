# Two-day media sprint source archive

This directory is append-only during generation. Each candidate belongs at:

```
<slot>/<candidate-id>/master.<source-extension>
<slot>/<candidate-id>/poster.png
<slot>/<candidate-id>/fallback.png
<slot>/<candidate-id>/metadata.json
```

The first Flow workspace is recorded in `data/media-sprint-jobs.json`. On
August 9, 2026, the account and project were accessible and three P0 reference
images uploaded, but Flow rejected a visibly populated prompt as empty. No
video candidate was created, so there is nothing to review or promote yet.

Do not start the weak-plate image queue until the P0 video generation issue is
resolved and the first candidates have been reviewed.
